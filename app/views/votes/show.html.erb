<p id="notice"><%= notice %></p>

<p>
  <strong>User:</strong>
  <%= @vote.user %>
</p>

<p>
  <strong>Post:</strong>
  <%= @vote.post %>
</p>

<p>
  <strong>Positive:</strong>
  <%= @vote.positive %>
</p>
<%= if current_user.id == @vote.user or current_user.admin_level != 0%>
  <%= link_to 'Edit', edit_vote_path(@vote) %> |
  <%= link_to 'Back', votes_path %>
<% end %>